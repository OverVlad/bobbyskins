import React, { Component } from 'react';

class ProfileSettings extends Component {
  render() {
    return (
      <div className="wrapper">Content for settings</div>
    );
  }
}

export default ProfileSettings;
